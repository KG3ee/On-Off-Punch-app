WEB_DOMAIN=app.example.com
API_DOMAIN=api.example.com

# Build-time env var — baked into the web client JS bundle by Next.js.
# Must be the public API URL that browsers will reach (not internal Docker hostname).
NEXT_PUBLIC_API_URL=https://api.example.com
